name: shared
description: "A new Flutter package project."
version: 0.0.1
homepage: "https://example.com/shared"

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  go_router_modular: ^4.0.0+7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
